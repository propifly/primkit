run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple

issues:
  exclude-rules:
    # Allow unused parameters in test files.
    - path: _test\.go
      linters:
        - unused
